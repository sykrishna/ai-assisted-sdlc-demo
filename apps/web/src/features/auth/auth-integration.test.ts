describe('Auth integration scaffold', () => {
  it.todo(
    'proxies login to the backend API and stores refresh state in an HttpOnly-compatible cookie',
  );
  it.todo('restores session state via refresh and session validation endpoints after reload');
  it.todo('clears cookie-backed refresh state and client auth state on logout');
  it.todo('returns standardized problem details when the backend auth dependency is unavailable');
});
