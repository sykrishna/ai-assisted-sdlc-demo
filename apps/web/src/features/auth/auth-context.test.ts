describe('AuthProvider scaffold', () => {
  it.todo('restores a session from the secure refresh flow on initial load');
  it.todo('clears auth state deterministically when refresh fails or logout completes');
  it.todo('prepares access-token refresh orchestration without using localStorage');
});
