# Terraform Modules

Reusable module placeholders for enterprise cloud foundations.

- `network/`: network and connectivity baseline.
- `security-baseline/`: IAM and security controls baseline.
- `observability-baseline/`: logs, metrics, and trace baseline.
- `compute-baseline/`: service runtime baseline.

Each module currently provides scaffold-only structure and no resource provisioning.
